# ROADMAP — 本地 AI Agent 永续开发协议

> 用户指令(2026-08-19):全权交给我持续推进,**我不说停就不要停止**。
> 停的条件只有:用户说「足够了不用继续开发了」或「暂停开发」。
> 按作息执行:9:00-12:00、14:00-18:00 休息,其余时间开发;休息前 10 分钟上线+更新版本。

## 阶段一:完成既定四大方向(当前)
1. **⏩ 持续提速** — 每轮顺带精简前置 token、调用开销
2. **🎨 UI 现代化迭代** — 每轮改进外观与交互
3. **📦 打包 exe/setup.exe + 安装文档** — PyInstaller 构建 LocalAgent.exe → Inno Setup 生成 setup.exe;AGENTS.md(给其他电脑 agent 读)已写好
4. **😴 作息自动化** — ✅ 已上线

## 阶段二:首轮总结与调研
1. **总结软件特点/优势/短板**(基于 V1-V17 基准 + 实际使用)。
2. **网络调研其它 agent**,重点是「适用于本地 LLM 的轻型 agent」:
   - 有哪些功能、缺陷(对照我们已有的)
   - **别人能做到的 → 我们做得更好更快**
   - **别人做不到的 → 我们想办法做到**
   - 调研来源要记录(URL),结论写入 `research/` 目录。

## 阶段三:永续迭代循环(核心)
```
每轮循环:
  1. 网络调研竞品(功能/缺陷/趋势)→ 记录到 research/
  2. 基于调研设计 3 个新版本更新(每版一个主题,难度/深度递增)
  3. 让 e4b + qwen3.5:4b + lfm2.5 三模型依次完成这 3 个版本迭代
     (每版:设计任务 → 三模型跑 → 记录结果到 bench/results.md → 失败则改进 agent 重试,最多 3 次)
  4. 根据迭代结果持续改进 agent 本体 + GUI
  5. 循环回步骤 1,永续进行
```
- 每个版本跑完记录到 `bench/results.md`。
- agent 改进记录到 `CHANGELOG.md`(版本号 +1)。
- 版本号策略:每个「3 版本批次」为一个小版本(+0.1)。

## 停止条件
- 用户明确说「足够了不用继续开发了」→ 停止开发,做最终总结。
- 用户说「暂停开发」→ 暂停,等待用户指示。
- 否则一直按作息持续。

## 各版本批次的记录
- 每完成一批 3 版本,更新 `VERSION` 与 `CHANGELOG.md`,并在 `research/` 写本轮调研小结。
